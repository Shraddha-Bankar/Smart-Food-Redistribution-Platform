Smart-Food-Redistribution-Platform/
│
├── public/                          # Static assets served as-is
│   ├── favicon.ico
│   ├── logo.png
│   └── images/                      # App images (banners, icons, etc.)
│
├── src/                              # Application source code
│   ├── assets/                       # Images, icons, fonts used inside components
│   ├── components/                   # Reusable UI components
│   │   ├── Navbar.jsx
│   │   ├── Footer.jsx
│   │   ├── DonationCard.jsx
│   │   └── Loader.jsx
│   ├── pages/                        # Route-level pages
│   │   ├── Home.jsx
│   │   ├── Login.jsx
│   │   ├── Register.jsx
│   │   ├── DonorDashboard.jsx
│   │   ├── NGODashboard.jsx
│   │   ├── AdminDashboard.jsx
│   │   └── About.jsx
│   ├── context/                      # React Context for auth / global state
│   │   └── AuthContext.jsx
│   ├── services/                     # API calls / backend integration
│   │   └── api.js
│   ├── utils/                        # Helper functions
│   │   └── helpers.js
│   ├── App.jsx                       # Root component, route definitions
│   ├── main.jsx                      # Vite entry point
│   └── index.css                     # Global styles
│
├── .env.example                      # Sample environment variables
├── .eslintrc.cjs                     # ESLint configuration
├── .gitignore                        # Git ignore rules
├── index.html                        # Vite HTML entry point
├── package.json                      # Project dependencies & scripts
├── package-lock.json                 # Locked dependency versions
├── vercel.json                       # Vercel deployment configuration
├── vite.config.js                    # Vite build configuration
└── README.md                         # Project documentation

-------------------------------------------------------------------------------
NOTE:
This structure.txt is reconstructed primarily from the visible GitHub
repository root (screenshot) — public/, src/, .env.example, .eslintrc.cjs,
.gitignore, index.html, package.json, package-lock.json, vercel.json,
vite.config.js — confirming this is a React + Vite project deployed on Vercel.

The internal contents of public/ and src/ shown above (pages, components,
context, services) are a REASONABLE ASSUMPTION based on typical structure
for a "Donor / NGO / Admin" food redistribution platform, since GitHub
blocks automated/script access to browse folder contents (robots.txt).

To get the EXACT structure, run this in your project root and paste the
output back to me, and I'll regenerate this file with 100% accuracy:

    On Windows (PowerShell):   tree /F
    On Mac/Linux:              find . -path ./node_modules -prune -o -print
                          or:  tree -I "node_modules"
